{{#each this}}
  <label for="{{name}}-{{max}}-{{full}}">
    <input
        id="{{name}}-{{max}}-{{full}}"
        type="radio"
        name="_{{name}}"
        value="{{max}}:{{full}}"
      />
    <span class="button button--neutral">
      {{#if full}}
      Full cycle:
      {{/if}}
      {{min}}–{{max}}
    </span>
  </label>
{{/each}}
